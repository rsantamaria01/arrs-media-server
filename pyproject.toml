[project]
name = "arrs-media-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "docker>=7.1.0",
  "pydantic>=2.12.5",
  "pydantic-settings>=2.13.1",
  "python-dotenv>=1.2.2",
]

[tool.uv]
package = false


[tool.uv.sources]

[tool.ty.environment]
root = ["./app"]
